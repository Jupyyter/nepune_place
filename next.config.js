/** @type {import('next').NextConfig} */
const nextConfig = {
       output: 'export',
       basePath: '/nepune_place',
       images: {
        //unoptimized: true,
      },
     }
     module.exports = nextConfig
